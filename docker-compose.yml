version: '3.8'

services:
  phantom-miner:
    build: .
    container_name: phantom-miner
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./config.json:/app/config.json
    environment:
      - TZ=UTC
